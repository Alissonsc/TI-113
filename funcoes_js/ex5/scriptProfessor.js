function menuLeitura() {
    let sair = false;

    do {
    let quantiLeitura = prompt(`
        Informe abaixo quantos números você deseja cadastras:
        A: 05 Números
        B: 15 Números
        C: 25 Números
        D: 50 Números
        `);

} while (!sair);
        opcao = opcao.toUpperCase();
        switch (opcao) {
            case "A":

                break;
            case "B":


        }
}